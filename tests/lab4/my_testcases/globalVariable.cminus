/* 测试全局变量以及同名变量作用域 */

int globalVariable;
int globalArray[10];

void main(void){
    /* 测试全局变量赋值与读取 */
    globalVariable = 1;
    output(globalVariable);
    globalArray[0] = 2;
    output(globalArray[0]);
}